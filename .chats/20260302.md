# Instructions

## Build SQS support entirely

look into @specs/rustack-sqs-design.md and also ./vendors code, think ultra hard to build sqs support entirely phase by phase, commit after each phase and once all finished use codex review skill to review the code against the spec and look into result carefully and fix all valid issues. Then build integration test and leverage existing integration test suite to validate sqs and then build docker image and relevant action (same as s3/dynamodb).

what is the 3rd party integration suite are you using?

do deep research for it

yes go ahead

## Fix SQS code review issues (Plan: pytest + boto3 SQS Conformance Test Suite)

make sure all fixed

Implement the following plan: pytest + boto3 SQS Conformance Test Suite

## Build SQS Docker image, update action and README

build docker image, update action (also action info, can you update action title as it is not just rustack s3), update read me, make sure everything works fine and the CI validate sqs as well
