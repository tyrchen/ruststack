# Instructions

## Fix versioning and minio-go Mint failures (Plan: Fix Versioning and minio-go)

Implement the following plan: Fix Versioning and minio-go Mint Test Failures - Object Lock enforcement in DeleteObject, aws-chunked Content-Encoding decoding

commit and push

go ahead don't ask

can you fix the code until these two test suite fully passed?

commit and push

## Proceed with Phase 1 improvements

go ahead with phase 1

make sure integration tests are all good and commit and push

## Create release and publish GitHub Action

can you create a release and publish the github action?

release failed look into CI logs and fix and tag and retry

Still failed! https://github.com/tyrchen/rustack/actions/runs/22534256490/job/65278646647

ok succeeded - how should others use the action?

have you published github action?

## Create S3 test workflow (Plan: S3 Test Workflow using Rustack GitHub Action)

can you create a s3 test workflow to use the rustack and do all kinds of s3 operations and see if things working well. This could also serve as example to other users.

Implement the following plan: Create S3 Test Workflow using Rustack GitHub Action

commit and push. Then trigger it and monitor

## Build Docker image and update documentation

have you built the latest docker image? can you update readme on how other users integrate this in their github action?

can you use gh action to build docker image?

can you monitor it and once it finished can you retrigger s3 test workflow and see if everything works fine

why this fails?

Only 1 annotation remains from a continue-on-error step (bucket config CORS API)

commit and push and trigger gh to build docker image

can you monitor it and once created trigger test s3 workflow

commit all and push

## Publish to GitHub Marketplace

is the action in github marketplace?

go ahead to create the release and push action and publish the release

done. is it published?

## Update README with comparison

can you update readme to compare this with python based localstack. And then push

## Write DynamoDB design spec

now as s3 is supported. Think ultra hard to support dynamodb in the same way (use smithy to gen interface code). Think ultra hard and write down a spec in ./specs for dynamodb support.
